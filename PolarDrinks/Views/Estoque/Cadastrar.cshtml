@model ProdutoModel
@{
    ViewData["title"] = "Cadastrar Produto";
}

<div class="tema-dark">
    <div class="container-fluid px-3 px-lg-4 py-4" style="max-width:1400px;">

        <div class="bg-shadow text-light p-4 rounded-4 mb-3 d-flex align-items-center gap-3">
            <div class="sf-icon-badge" style="background:linear-gradient(135deg,#3b82f6,#6366f1); box-shadow:0 8px 20px -8px rgba(59,130,246,.8);">
                <i class="bi bi-box-seam-fill fs-4 text-white"></i>
            </div>
            <div>
                <h1 class="m-0 fs-3 fw-bold">Cadastrar Produto</h1>
            </div>
        </div>

        <div class="bg-shadow text-light p-4 rounded-4">
            @if (TempData["MensagemSucesso"] != null)
            {
                <div class="bg-shadow text-light p-3 rounded-4 mb-3" style="background:rgba(16,185,129,.15) !important; border-left:3px solid #10b981 !important;">
                    <i class="bi bi-check-circle-fill me-2" style="color:#34d399;"></i>
                    @TempData["MensagemSucesso"]
                </div>
            }
            else if (TempData["MensagemErro"] != null)
            {
                <div class="bg-shadow text-light p-3 rounded-4 mb-3" style="background:rgba(239,68,68,.15) !important; border-left:3px solid #ef4444 !important;">
                    <i class="bi bi-exclamation-circle-fill me-2" style="color:#f87171;"></i>
                    @TempData["MensagemErro"]
                </div>
            }

            <form method="post" asp-controller="Estoque" asp-action="Cadastrar" autocomplete="off" enctype="multipart/form-data">
                <div class="mb-4">
                    <label class="form-label text-light">Nome</label>
                    <input type="text" class="form-control bg-shadow text-light" asp-for="ProdutoNome" placeholder="Nome do Produto" maxlength="100">
                    <span asp-validation-for="ProdutoNome" class="text-danger"></span>
                </div>

                <div class="mb-4">
                    <label class="form-label text-light">Descrição</label>
                    <textarea class="form-control bg-shadow text-light" asp-for="ProdutoDescricao" rows="3" placeholder="Descrição do Produto" maxlength="200"></textarea>
                    <span asp-validation-for="ProdutoDescricao" class="text-danger"></span>
                </div>

                <div class="row mb-4">
                    <div class="col-md-4">
                        <label class="form-label text-light">Código de Barras</label>
                        <input type="text" class="form-control bg-shadow text-light" asp-for="ProdutoCodBarra" placeholder="Ex: 1234567890123" maxlength="13">
                        <span asp-validation-for="ProdutoCodBarra" class="text-danger"></span>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label text-light">Preço de Custo</label>
                        <input type="text" class="form-control bg-shadow text-light" asp-for="ProdutoPrecoCusto" placeholder="0.00">
                        <span asp-validation-for="ProdutoPrecoCusto" class="text-danger"></span>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label text-light">Preço de Venda</label>
                        <input type="text" class="form-control bg-shadow text-light" asp-for="ProdutoPrecoVenda" placeholder="0.00">
                        <span asp-validation-for="ProdutoPrecoVenda" class="text-danger"></span>
                    </div>
                </div>

                <div class="row mb-4">
                    <div class="col-md-6">
                        <label class="form-label text-light">Quantidade em Estoque</label>
                        <input type="number" class="form-control bg-shadowi text-light" asp-for="ProdutoQtdEstoque" value="0" readonly>
                        <span asp-validation-for="ProdutoQtdEstoque" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label text-light">Estoque Mínimo</label>
                        <input type="text" class="form-control bg-shadow text-light" asp-for="ProdutoEstoqueMinimo" placeholder="0" maxlength="10">
                        <span asp-validation-for="ProdutoEstoqueMinimo" class="text-danger"></span>
                    </div>
                </div>

                <div class="mb-4">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" asp-for="ProdutoAtivo">
                        <label class="form-check-label text-light">Produto disponível</label>
                    </div>
                </div>

                <div class="mb-4">
                    <label class="form-label text-light">Imagem do Produto</label>
                    <input type="file" class="form-control bg-shadow text-light" name="imagem" accept="image/*">
                </div>

                <div class="mb-4">
                    <label class="form-label text-light d-block">Categorias</label>
                    @foreach (var categoria in (List<PolarDrinks.Models.Loja.CategoriaModel>)ViewBag.TodasCategorias)
                    {
                        <div class="form-check form-check-inline">
                            <input class="form-check-input" type="checkbox" name="categoriaIds" value="@categoria.CategoriaID">
                            <label class="form-check-label text-light">@categoria.CategoriaNome</label>
                        </div>
                    }
                </div>

                <div class="d-flex gap-3 mt-4">
                    <a class="btn btn-outline-light w-50" asp-controller="Estoque" asp-action="Index" title="Voltar">
                        <i class="bi bi-arrow-left me-1"></i>Voltar
                    </a>
                    <button type="submit" class="btn btn-success w-50" title="Cadastrar">
                        <i class="bi bi-plus-lg me-1"></i>Cadastrar
                    </button>
                </div>
            </form>
        </div>

    </div>
</div>

@section Scripts {
<script>

    $(document).on("input", 'input[name="ProdutoPrecoCusto"], input[name="ProdutoPrecoVenda"]', function () {
        let value = this.value;


        value = value.replace(/[^0-9.]/g, '');


        let parts = value.split('.');
        if (parts.length > 2) {
            value = parts[0] + '.' + parts[1];
        }


        if (value.startsWith('0') && value.length > 1 && value[1] !== '.') {
            value = value.replace(/^0+/, '');
        }


        if (value.includes('.')) {
            let [intPart, decPart] = value.split('.');
            intPart = intPart.substring(0, 5);
            decPart = decPart.substring(0, 2);
            value = intPart + '.' + decPart;
        } else {
            value = value.substring(0, 5);
        }

        this.value = value;
    });


    $(document).on("input", 'input[name="ProdutoEstoqueMinimo"]', function () {
        let value = this.value;


        value = value.replace(/[^0-9]/g, '');


        value = value.substring(0, 4);


        if (value.length > 1) {
            value = value.replace(/^0+/, '');
        }

        this.value = value;
    });


    $(document).on("input", 'input[name="ProdutoCodBarra"]', function () {
        let value = this.value;


        value = value.replace(/[^0-9]/g, '');

        this.value = value;
    });

</script>
}